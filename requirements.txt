PySide6>=6.6
numpy>=1.26
opencv-python>=4.9

# EXR I/O goes through OpenImageIO (import OpenImageIO as oiio), not the
# OpenEXR package. OpenImageIO's Python bindings are not reliably
# pip-installable on Windows; install via conda-forge instead:
#   conda install -c conda-forge openimageio
